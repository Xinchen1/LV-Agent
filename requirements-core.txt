# Core runtime dependencies — enough to start the agent with a cloud LLM backend.
# Heavy optional deps (torch, chromadb, sentence-transformers) are NOT included
# here so the agent can start on any machine without long compile/download times.
# Install requirements-minimal.txt for full memory/embedding features.

openai>=1.0.0
pyyaml>=6.0
pydantic>=2.5.0
requests>=2.31.0
python-dotenv>=1.0.0
rich>=13.7.0
tqdm>=4.66.0
tiktoken>=0.5.0
beautifulsoup4>=4.12.0
